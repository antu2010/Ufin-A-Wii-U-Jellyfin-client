stb_image.h from https://github.com/nothings/stb (commit 2c980bb59875b0d32144a71867fbdebb2f77cd20),
unmodified. Public domain / MIT (dual-licensed; see the end of the file).
Used by src/image_cache.cpp with STBI_ONLY_JPEG and STBI_ONLY_PNG.

tests/host/vendor/stb_image_write.h (same repository and licence) is used by
the host tests only, to produce JPEG/PNG bytes.
